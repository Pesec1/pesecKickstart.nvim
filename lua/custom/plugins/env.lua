return {
  --   'ellisonleao/dotenv.nvim',
  --   config = function()
  --     require('dotenv').setup {
  --       enable_on_load = true,
  --       varbose = false,
  --     }
  --   end,
}
--TODO does not work with debugger for now, so added everything in bashrc
